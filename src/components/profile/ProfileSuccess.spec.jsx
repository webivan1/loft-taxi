import { render } from '@testing-library/react'
import { ProfileSuccess } from './ProfileSuccess'

describe('ProfileSuccess', () => {
  it('should render component', () => {
    render(<ProfileSuccess />)
  })
})